1. node version 20

`yarn install`
`yarn dev`

2. url to view house 2

`http://localhost:5173/?id=gm-house`

3. url to view home 2

`http://localhost:5173/?id=gm-house2`

4. id is home file name without extension which must exist in public/sh3d/

5. files should be exported from SweetHome3D as `Save and Compress`

TODO: 
1. add a page where user can upload files and a list of uploaded files is shown with links to above page where clients of the user can view their home designs.
2. file name shoul be changed to uuid after upload so that clients can not guess other home names.
3. make pages look better with header and footer for xoredge